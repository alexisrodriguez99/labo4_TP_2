import { NavHoverDirective } from './nav-hover.directive';

describe('NavHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NavHoverDirective();
    expect(directive).toBeTruthy();
  });
});
