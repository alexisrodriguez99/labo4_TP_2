import { FechaDirective } from './fecha.directive';

describe('FechaDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaDirective();
    expect(directive).toBeTruthy();
  });
});
