import { OrdenarPorDiaPipe } from './ordenar-por-dia.pipe';

describe('OrdenarPorDiaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorDiaPipe();
    expect(pipe).toBeTruthy();
  });
});
