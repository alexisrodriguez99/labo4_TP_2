import { FechaProgramadaPipe } from './fecha-programada.pipe';

describe('FechaProgramadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaProgramadaPipe();
    expect(pipe).toBeTruthy();
  });
});
